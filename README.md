# step to create git repo
- login to GitHub account
- click on create new
- create a repo name
   - git init
   - git add .
   - git commit -m "initial commit"
   - git branch
   - git branch -M main
   - git remote add origin `link`
   - git push origin  main
   
